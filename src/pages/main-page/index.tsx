import { Box } from "@mui/material";

export default function MainPage() {
    return (
        <Box component='section' sx={{ height: '100vh' }}>
            <Box sx={{ backgroundColor: 'coral', width: '100%', height: '100%' }}>
                Main Page
            </Box>
        </Box>
    );
}